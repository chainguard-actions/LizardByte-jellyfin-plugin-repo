name: Test add action input validation
on:
  workflow_dispatch:
jobs:
  test-add-validation:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - name: Run action with valid add action value
        id: action
        uses: .
        continue-on-error: true
        with:
          action: add
          committer_email: test@example.com
          committer_name: Test User
          github_token: fake-token
          release_tag: v1.0.0
          repository: LizardByte/jellyfin-plugin-repo

      - name: Assert action did not fail on input validation
        env:
          OUTCOME: ${{ steps.action.outcome }}
        run: |
          echo "outcome: $OUTCOME"
          # The action should pass input validation (action=add is valid)
          # It may fail later at network steps (checkout, push), but should NOT
          # fail at the Setup inputs step with "Invalid action" error.
          # We check that if it failed, it was NOT due to invalid action input.
          # Since we can't distinguish failure reasons easily, we just verify
          # the action ran (outcome is either success or failure, not skipped).
          if [ "$OUTCOME" == "skipped" ]; then
            echo "FAIL: action was skipped unexpectedly"
            exit 1
          fi
          echo "PASS: action ran with valid 'add' input (outcome=$OUTCOME)"
